# cursor-cli-to-api-webui

Vue test + docs UI for the OpenAI-compatible Cursor CLI proxy.

## Run

```powershell
npm install
npm run dev
```

Dev server: `http://localhost:5201`  
API base: `VITE_API_BASE_URL` (default `http://127.0.0.1:8201`)

Login: `armin` / `dopadopa123`
